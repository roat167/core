package io.kapp.core.java.basic;

public class PrimitiveType {

/**
 * Primitives always have a value, they can never be null
 * If not defined they will take default value 0, and false for boolean
 * Suffix: Good practice is to use uppercase 
 *	L for long
 * 	F for float 
 * 	D for double (optional)
 *
 * Primitives and size in BITS
 * 	boolean		1
 * 	byte		8	(1 byte)
 * 	short		16	(2 byte)
 * 	int 		32	(4 byte)
 * 	long 		64	(8 byte)
 * 	float		32	(4 byte)
 * 	double 		64	(8 byte)
 * 	char		16 (unsigned) default value '\u0000'
 */

	public static void main(String[] args) {
		//int val = Integer.MAX_VALUE;
		//long biggerVal = val + 1;
		
	}
}
