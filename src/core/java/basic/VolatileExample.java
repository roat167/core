package io.kapp.core.java.basic;

/**
 * volatile is used to indicate that a variable's value will be modified by
 * different threads. Variable will never be cached thread-locally: all reads
 * and writes will go straight to "main memory"; Access to the variable acts as
 * though it is enclosed in a synchronized block, synchronized on itself.
 */
public class VolatileExample {

}
